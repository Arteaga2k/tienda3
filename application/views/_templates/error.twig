{% extends "_templates/base.twig" %} {% block content %}
<div class="page-header">
	<h1>
		{{ tabla }} <small>{{ cabecera }} #{{ id }}</small>
	</h1>

</div>
<p>{{ mensaje }}</p>
	<a class="btn btn-default btn-lg btn-block"
		href="{{ constant('URL') }}{{ tabla }}">Aceptar</a>

{% endblock %}
